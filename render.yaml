services:
  - type: web           # <-- use "web", not "static"
    name: my-cdn
    env: static         # tells Render this is a static site
    buildCommand: ""    # nothing to build
    staticPublishPath: "/"   # the folder to publish (root of repo)
